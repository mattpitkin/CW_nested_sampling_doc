\newcommand{\ee}[1]{\!\times\!10^{#1}}
\newcommand{\gws}{gravitational waves\xspace}
\newcommand{\gw}{gravitational wave\xspace}
\newcommand{\lalinf}{\texttt{LALInference}\xspace}

\bibliographystyle{aipauth4-1}

\iflatexml
  % define things that are only required for rendering on authorea
  
  % call natbib so LaTeXML can use citet and citep commands
  \usepackage{natbib}

  % define the widetext environment as empty otherwise tags show up on authorea
  \newenvironment{widetext}{}{}
\fi
